Given a repository and an uri to a yml pipeline, i need to be able to lookup in ado which "physical" pipelines, if any, I have using that pipeline in ADO

Invoke-CliCommand must execute as default in the repository root - and must support invoke-in-folder parm
